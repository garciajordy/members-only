<h1> Posts </h1>

<% @posts.each do |post| %>
<% if user_signed_in? %>
   <% @user = User.find(post.user_id)%> 
   <%= @user.username%>

<% end%>
<h2><%= post.title%></h2>
<p><%= post.body%></p>
<% end %>